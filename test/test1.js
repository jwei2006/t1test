const test = require("tester");
let mocha = require('mocha');
describe('1', function () {
    it('should ', function () {
        let result = test.func.get("should", "http://test-activity.changyou.com/test/token?guid=1", {}, function () {

        }, done);
    });
});